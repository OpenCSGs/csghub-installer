{{- /*
Copyright OpenCSG, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.custom" (list . "aigateway") }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "common.labels" (dict "context" . "service" "aigateway") | nindent 4 }}
spec:
  type: {{ .Values.csghub.aigateway.service.type }}
  ports:
    - port: {{ .Values.csghub.aigateway.service.port }}
      targetPort: 8084
      protocol: TCP
      name: aigateway
  selector:
    {{- include "common.serviceSelectorLabels" (dict "selector" true "context" . "service" "aigateway") | nindent 4 }}