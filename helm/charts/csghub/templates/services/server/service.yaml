{{- /*
Copyright OpenCSG, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.custom" (list . "server") }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "common.labels" (dict "context" . "service" "server") | nindent 4 }}
spec:
  type: {{ .Values.csghub.server.service.type }}
  ports:
    - port: {{ .Values.csghub.server.service.port }}
      targetPort: 8080
      protocol: TCP
      name: server
  selector:
    {{- include "common.serviceSelectorLabels" (dict "selector" true "context" . "service" "server") | nindent 4 }}