{{- /*
Copyright OpenCSG, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.custom" (list . "notification") }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "common.labels" (dict "context" . "service" "notification") | nindent 4 }}
spec:
  type: {{ .Values.csghub.notification.service.type }}
  ports:
    - port: {{ .Values.csghub.notification.service.port }}
      targetPort: 8095
      protocol: TCP
      name: notification
  selector:
    {{- include "common.serviceSelectorLabels" (dict "selector" true "context" . "service" "notification") | nindent 4 }}